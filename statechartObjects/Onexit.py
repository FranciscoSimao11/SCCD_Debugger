class Onexit():
    def __init__(self, script, log, raiseEvent):
                self.script = script
                self.log = log
                self.raiseEvent = raiseEvent
               
    def getPrintableObject(self):
        return ""
